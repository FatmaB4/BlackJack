/*
*
*  This is the tester class for the Player Class.  Copy all of the code starting with
*  line 9 to the end of the file.  Open up Main.java and replace all of the code in
*  Main.java with this code.  Then run it to test the Player Class.
*/

/*
import static java.lang.System.*;
import java.awt.Color;

public class Main
{
	public static void main( String args[] )
	{
		Player player = new Player();	
		
		Deck deck = new Deck();
		deck.shuffle();
		
		player.addCardToHand(deck.nextCard());
		player.addCardToHand(deck.nextCard());

		out.println("\n\ntoString");
		out.println(player);	
		
		out.println("\n\nhandValue");
		out.println(player.getHandValue());					
		
		player.addCardToHand(deck.nextCard());
		player.addCardToHand(deck.nextCard());

		out.println("\n\ntoString");
		out.println(player);	
		
		out.println("\n\nhandValue");
		out.println(player.getHandValue());									
	}
}
*/